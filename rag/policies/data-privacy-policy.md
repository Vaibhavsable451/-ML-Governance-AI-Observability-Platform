# Data Privacy Policy

Personally identifiable information must never be sent to a model unredacted. Detected PII includes email addresses, phone numbers, PAN, Aadhaar, credit card numbers and social security numbers.

Detected PII is replaced with a typed placeholder before retrieval and generation. PII detections are logged without the raw value.

Training and reference datasets must not contain direct identifiers.
